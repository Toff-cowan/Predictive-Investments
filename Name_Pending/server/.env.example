# Server (optional – defaults work for local dev)
# Login/signup require a DB; run: npm run db:push (from repo root)
#DATABASE_URL=postgresql://localhost:5432/name_pending
#CORS_ORIGIN=http://localhost:5173
#NODE_ENV=development
